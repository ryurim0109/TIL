const trap = (height) => {
  const len = height.length;
  let stack = [];
  let answer = 0;
  for (let i = 0; i < len; i++) {}
};

const height = [0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1];
console.log(trap(height));
